extends ../../layout/auth.pug

include ../../mixins/showalert.pug

block main

    +alertError(5000)

    .d-flex.justify-content-center.align-items-center(style="min-height: 80vh;")
        .col-11.col-sm-8.col-md-6.col-lg-4
            .card.shadow-sm
                .card-body.p-4
                    .text-center.mb-4
                        img(src="/images/logo_duyTan.png", alt="Logo" style="max-width: 80px; height: auto;")
                        h4.mt-3.mb-1.text-uppercase.text-muted Quản trị hệ thống
                        h5.mb-0.text-dark.fw-bold Đăng nhập
                        p.text-muted.mt-1(style="font-size: .9rem;") Vui lòng nhập email và mật khẩu để tiếp tục.

                    form(
                        action=`${prefixAdmin}/auth/login`
                        method="POST"
                    )

                        //- Email
                        div(class="form-group")
                            label.form-label(for="email") Email
                            input(
                                type="email"
                                name="email"
                                class="form-control"
                                id="email"
                                placeholder="Nhập email"
                                required
                            )

                        //- Mật khẩu
                        div(class="form-group")
                            label.form-label(for="password") Mật khẩu
                            input(
                                type="password"
                                name="password"
                                class="form-control"
                                id="password"
                                placeholder="Nhập mật khẩu"
                                required
                            )

                        //- Ghi nhớ đăng nhập + Quên mật khẩu (tuỳ chọn)
                        //- .d-flex.justify-content-between.align-items-center.mb-3
                        //-     .form-check
                        //-         input#remember.form-check-input(type="checkbox" name="remember")
                        //-         label.form-check-label(for="remember") Ghi nhớ đăng nhập
                        //-     a(href="#", style="font-size: .85rem;") Quên mật khẩu?

                        div(class="form-group")
                            button(
                                style="margin-top: 10px;"
                                class="btn btn-primary w-100"
                                type="submit"
                            ) Đăng nhập

                        //- Thông tin phụ
                        p.text-center.text-muted.mt-3(style="font-size: .85rem;")
                            | Bảo mật tài khoản của bạn, không chia sẻ thông tin đăng nhập cho người khác.

